# Rapport compilé

Les rapports compilés sont disponibles dans ce dossier :

- `main.pdf` : rapport principal avec le chapitre 3 intégré, 82 pages ;
- `name.pdf` : version alternative, 34 pages.

Les sources correspondantes sont `main.tex` et `name.tex`. La compilation a
été réalisée avec XeLaTeX. La configuration linguistique utilise
`polyglossia` avec le français.
